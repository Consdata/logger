{
  "name": "@consdata/logger-demo",
  "private": true,
  "version": "0.1.1",
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "lib-upgrade": "yarn upgrade-interactive --latest",
    "test": "jest --passWithNoTests",
    "lint": "tslint 'src/**/*.ts' -p src/tsconfig.lib.json",
    "prebuild": "rimraf .build && rimraf dist",
    "postbuild": "rimraf .build",
    "build:compile": "tsc -p src/tsconfig.lib.json",
    "build:test": "yarn test --coverage --coverageDirectory '../dist/coverage'",
    "build:lint": "yarn lint",
    "build": "yarn build:compile && yarn build:lint && yarn build:test"
  },
  "dependencies": {
    "@consdata/logger-api": "^0.1.1",
    "@consdata/logger-console": "^0.1.1"
  },
  "devDependencies": {
    "@types/jest": "24.0.9",
    "concurrently": "4.1.0",
    "copyfiles": "2.1.0",
    "coveralls": "3.0.3",
    "jest": "24.1.0",
    "rimraf": "2.6.3",
    "rollup": "1.3.3",
    "rollup-plugin-cleanup": "^3.1.1",
    "ts-jest": "24.0.1",
    "tslint": "5.13.0",
    "typedoc": "0.15.0",
    "typescript": "3.3.3333"
  }
}
