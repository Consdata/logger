# logger-console

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build logger-console` to build the library.

## Running unit tests

Run `nx test logger-console` to execute the unit tests via [Jest](https://jestjs.io).
