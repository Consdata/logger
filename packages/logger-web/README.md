# logger-web

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build logger-web` to build the library.

## Running unit tests

Run `nx test logger-web` to execute the unit tests via [Jest](https://jestjs.io).
